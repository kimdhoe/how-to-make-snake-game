import React from 'react'

const DataType = ({ text }) => (
  <span
    css={{
      fontWeight: 600,
      fontStyle: 'italic',
      fontFamily: 'Georgia',
    }}
  >
    {text}
  </span>
)

export default DataType
